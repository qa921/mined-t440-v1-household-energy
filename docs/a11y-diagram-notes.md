# Diagram accessibility notes — M-T440-V1

**Review date:** 2026-09-11

The visual must expose a concise text equivalent in the active language. It must name the three nodes in order and make the direction explicit: baseline conditions → operator assumption → projected scenario. Decorative arrows alone are insufficient. Local market context may appear in supporting copy, not as a fourth causal node.

Open visual checks after implementation: contrast of labels, arrow direction at narrow widths, and whether long Dutch labels wrap without hiding the connector.
